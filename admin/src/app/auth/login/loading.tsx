// src/app/(auth)/login/loading.tsx

import React from 'react';

const Loading = () => {
  return (
    <div style={{ textAlign: 'center', padding: '20px' }}>
      <h2>Cargando...</h2>
      <p>Estamos cargando la página, por favor espera un momento.</p>
    </div>
  );
};

export default Loading;